import myMixin from './myMixin';

export default myMixin;


/*
Vue.mixin({
    methods: {
        logout: function () {
            this.$localStorage.clear();
            this.$router.push({ name: 'login' });
        }
    }
});
*/
